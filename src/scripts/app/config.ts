
export class Config {
    // Conversion from pixels to physics coordinates.  0.25 means 4 pixels are 1 meter.
    static physicsScale = 0.25;

    static gravity = 1.62;

    static playerThrust = 750;
    static tileSize = 32;
    static turretTurnRate = 1;
    static shotSpeed = 75;
    static shotDelay = 0.5;
    static recoil = 3000;
    static shotDuration = 30;
    static helpColor = 0xddddff;
}
